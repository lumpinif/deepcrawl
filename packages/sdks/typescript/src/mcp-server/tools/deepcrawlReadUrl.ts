/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deepcrawlReadUrl } from "../../funcs/deepcrawlReadUrl.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ReadUrlRequest$inboundSchema,
};

export const tool$deepcrawlReadUrl: ToolDefinition<typeof args> = {
  name: "deepcrawl-read-url",
  description: `Returning full result object from the request URL.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deepcrawlReadUrl(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
