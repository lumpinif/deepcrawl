{
  "name": "@deepcrawl-worker/supabase",
  "private": true,
  "scripts": {
    "lint": "biome lint",
    "format": "biome format --write",
    "check": "biome check --write --unsafe",
    "lint:fix": "biome lint --write --unsafe",
    "fix": "pnpm lint && pnpm format && pnpm check",
    "db:gen-types": "pnpx supabase gen types typescript --project-id $PROJECT_REF --schema public > supabase/types/database.types.ts"
  }
}
