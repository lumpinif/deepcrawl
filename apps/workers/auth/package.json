{
  "name": "deepcrawl_auth",
  "scripts": {
    "dev": "wrangler dev --remote --env=development",
    "dev:staging": "wrangler dev --remote --env=staging",
    "dev:prod": "wrangler dev --remote",
    "deploy": "pnpm run cf-typegen && pnpm run db:sync:prod && wrangler secret bulk .dev.vars.production && wrangler deploy --minify",
    "check": "biome check --write && pnpm typecheck",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings && pnpm typecheck",
    "db:sync": "turbo run db:sync --filter=@deepcrawl/db",
    "db:sync:prod": "turbo run db:sync:prod --filter=@deepcrawl/db",
    "db:generate": "turbo run db:generate --filter=@deepcrawl/db",
    "db:migrate": "turbo run db:migrate --filter=@deepcrawl/db",
    "db:migrate:prod": "turbo run db:migrate:prod --filter=@deepcrawl/db",
    "auth:generate": "turbo run auth:generate --filter=@deepcrawl/auth"
  },
  "dependencies": {
    "@deepcrawl/auth": "workspace:*",
    "@deepcrawl/db": "workspace:*",
    "@neondatabase/serverless": "^1.0.0",
    "drizzle-orm": "^0.44.1",
    "hono": "^4.7.10",
    "stoker": "^1.4.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250531.0",
    "drizzle-kit": "^0.31.1"
  }
}
