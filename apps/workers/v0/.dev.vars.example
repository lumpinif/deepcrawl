# Used by Wrangler in local development (secrets only)
# In production, these should be set as Cloudflare Worker Secrets.
# Non-secrets are configured via Wrangler vars (see wrangler.jsonc).

# Auth
# Better Auth secret. Required when AUTH_MODE=better-auth.
BETTER_AUTH_SECRET=

# Postgres connection string (Neon, etc). Required when AUTH_MODE=better-auth.
DATABASE_URL=

# JWT
# JWT secret used by the API worker when AUTH_MODE=jwt.
JWT_SECRET=

# OAuth
# GitHub OAuth client secret.
GITHUB_CLIENT_SECRET=

# Google OAuth client secret.
GOOGLE_CLIENT_SECRET=

# Email
# Resend API key (optional).
RESEND_API_KEY=

# Upstash
# Upstash Redis REST URL (used by API rate limiting). Required only when ENABLE_API_RATE_LIMIT=true.
UPSTASH_REDIS_REST_URL=

# Upstash Redis REST token (used by API rate limiting). Required only when ENABLE_API_RATE_LIMIT=true.
UPSTASH_REDIS_REST_TOKEN=

# Cloudflare
# Cloudflare account id (for D1 Drizzle CLI).
CLOUDFLARE_ACCOUNT_ID=

# Cloudflare D1 database id (for D1 Drizzle CLI).
CLOUDFLARE_DATABASE_ID=

# Cloudflare API token for D1 Drizzle CLI.
CLOUDFLARE_D1_TOKEN=
