{
  "name": "deepcrawl",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format && biome format --write .",
    "lint:repo": "pnpm dlx sherif@latest",
    "lint:repo:fix": "pnpm dlx sherif@latest --fix",
    "fix": "turbo lint && turbo fix && pnpm format && turbo typecheck && pnpm run lint:repo:fix"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@deepcrawl/eslint-config": "workspace:*",
    "@deepcrawl/typescript-config": "workspace:*",
    "prettier": "^3.5.1",
    "turbo": "^2.4.2",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">=20"
  }
}
