# Single source of truth for local development.
#
# 1) Copy this file to env/.env
# 2) Fill in values
# 3) Run: pnpm env:sync:local
#
# Note: This file is NOT read by Next.js or Wrangler directly.

# App
# Public app URL (used for navigation and auth callbacks).
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Public API base URL for the dashboard server to call.
NEXT_PUBLIC_DEEPCRAWL_API_URL=https://api.deepcrawl.dev

# Auth
# Set to 'false' to use integrated Next.js auth routes instead of the auth worker.
NEXT_PUBLIC_USE_AUTH_WORKER=true

# Auth base URL. Points to auth worker or to Next.js /api/auth depending on mode.
BETTER_AUTH_URL=http://localhost:8787

# Public auth base URL for the browser bundle. If omitted, `pnpm env:sync:local` will auto-fill it from `BETTER_AUTH_URL`.
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:8787

# Better Auth secret.
BETTER_AUTH_SECRET=

# Postgres connection string (Neon, etc).
DATABASE_URL=

# Auth mode: better-auth | jwt | none.
AUTH_MODE=better-auth

# JWT
# JWT token used by the dashboard server when AUTH_MODE=jwt.
AUTH_JWT_TOKEN=

# JWT secret used by the API worker when AUTH_MODE=jwt.
JWT_SECRET=

# JWT issuer (optional).
JWT_ISSUER=

# JWT audience (optional).
JWT_AUDIENCE=

# Logs
# Enable activity log persistence.
ENABLE_ACTIVITY_LOGS=true

# OAuth
# GitHub OAuth client id.
GITHUB_CLIENT_ID=

# GitHub OAuth client secret.
GITHUB_CLIENT_SECRET=

# Google OAuth client id (public).
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Google OAuth client secret.
GOOGLE_CLIENT_SECRET=

# Email
# Resend API key (optional).
RESEND_API_KEY=

# From email, e.g. "Deepcrawl <noreply@deepcrawl.dev>".
FROM_EMAIL=

# Upstash
# Upstash Redis REST URL (used by rate limiting).
UPSTASH_REDIS_REST_URL="****"

# Upstash Redis REST token (used by rate limiting).
UPSTASH_REDIS_REST_TOKEN="****"

# Cloudflare
# Cloudflare account id (for D1 Drizzle CLI).
CLOUDFLARE_ACCOUNT_ID=

# Cloudflare D1 database id (for D1 Drizzle CLI).
CLOUDFLARE_DATABASE_ID=

# Cloudflare API token for D1 Drizzle CLI.
CLOUDFLARE_D1_TOKEN=

# Turbo
# Turbo remote cache token (CI only).
TURBO_TOKEN=

# SDK
# SDK API key used by examples (server-only).
DEEPCRAWL_API_KEY=dc-your-generated-key

# SDK API base URL override (optional).
DEEPCRAWL_API_URL=
