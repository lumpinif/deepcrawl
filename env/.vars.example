# Wrangler vars (non-secrets).
#
# 1) Copy this file to env/.vars
# 2) Fill in values
# 3) Run: pnpm env:bootstrap
#
# Local values write to wrangler.jsonc "vars".
# PRODUCTION__* values write to wrangler.jsonc "env.production.vars".

# App
# Public app URL (used for navigation and auth callbacks).
NEXT_PUBLIC_APP_URL=http://localhost:3000
PRODUCTION__NEXT_PUBLIC_APP_URL=https://deepcrawl.dev

# Auth
# Auth base URL.
BETTER_AUTH_URL=http://localhost:8787
PRODUCTION__BETTER_AUTH_URL=https://auth.deepcrawl.dev

# Auth mode: better-auth | jwt | none.
AUTH_MODE=better-auth
PRODUCTION__AUTH_MODE=better-auth

# JWT
# JWT issuer (optional).
JWT_ISSUER=
PRODUCTION__JWT_ISSUER=

# JWT audience (optional).
JWT_AUDIENCE=
PRODUCTION__JWT_AUDIENCE=

# Workers
# Public API base URL (used for links and OpenAPI metadata).
API_URL=http://localhost:8080
PRODUCTION__API_URL=https://api.deepcrawl.dev

# Node env for the API worker.
WORKER_NODE_ENV=development
PRODUCTION__WORKER_NODE_ENV=production

# Node env for the auth worker.
AUTH_WORKER_NODE_ENV=development
PRODUCTION__AUTH_WORKER_NODE_ENV=production

# Logs
# Enable activity logs (non-secret toggle).
ENABLE_ACTIVITY_LOGS=true
PRODUCTION__ENABLE_ACTIVITY_LOGS=true

# OAuth
# GitHub OAuth client id.
GITHUB_CLIENT_ID=
PRODUCTION__GITHUB_CLIENT_ID=

# Google OAuth client id (public).
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
PRODUCTION__NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Email
# From email address.
FROM_EMAIL=
PRODUCTION__FROM_EMAIL=
